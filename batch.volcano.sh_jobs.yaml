
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.0
  creationTimestamp: null
  name: jobs.batch.volcano.sh
spec:
  group: batch.volcano.sh
  names:
    kind: Job
    listKind: JobList
    plural: jobs
    shortNames:
    - vcjob
    - vj
    singular: job
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state.phase
      name: STATUS
      type: string
    - jsonPath: .status.minAvailable
      name: minAvailable
      type: integer
    - jsonPath: .status.running
      name: RUNNINGS
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .spec.queue
      name: QUEUE
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Job defines the volcano job.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Specification of the desired behavior of the volcano job,
              including the minAvailable
            properties:
              maxRetry:
                default: 3
                description: Specifies the maximum number of retries before marking
                  this Job failed. Defaults to 3.
                format: int32
                type: integer
              minAvailable:
                description: The minimal available pods to run for this Job Defaults
                  to the summary of tasks' replicas
                format: int32
                type: integer
              minSuccess:
                description: The minimal success pods to run for this Job
                format: int32
                minimum: 1
                type: integer
              networkTopology:
                description: NetworkTopology defines the NetworkTopology config, this
                  field works in conjunction with network topology feature and hyperNode
                  CRD.
                properties:
                  highestTierAllowed:
                    default: 1
                    description: HighestTierAllowed specifies the highest tier that
                      a job allowed to cross when scheduling.
                    type: integer
                  mode:
                    default: hard
                    description: Mode specifies the mode of the network topology constrain.
                    enum:
                    - hard
                    - soft
                    type: string
                type: object
              plugins:
                additionalProperties:
                  items:
                    type: string
                  type: array
                description: Specifies the plugin of job Key is plugin name, value
                  is the arguments of the plugin
                type: object
              policies:
                description: Specifies the default lifecycle of tasks
                items:
                  description: LifecyclePolicy specifies the lifecycle and error handling
                    of task and job.
                  properties:
                    action:
                      description: The action that will be taken to the PodGroup according
                        to Event. One of "Restart", "None". Default to None.
                      type: string
                    event:
                      description: The Event recorded by scheduler; the controller
                        takes actions according to this Event.
                      enum:
                      - '*'
                      - PodPending
                      - PodRunning
                      - PodFailed
                      - PodEvicted
                      - Unknown
                      - TaskCompleted
                      - OutOfSync
                      - CommandIssued
                      - JobUpdated
                      - TaskFailed
                      type: string
                    events:
                      description: The Events recorded by scheduler; the controller
                        takes actions according to this Events.
                      items:
                        description: Event represent the phase of Job, e.g. pod-failed.
                        enum:
                        - '*'
                        - PodPending
                        - PodRunning
                        - PodFailed
                        - PodEvicted
                        - Unknown
                        - TaskCompleted
                        - OutOfSync
                        - CommandIssued
                        - JobUpdated
                        - TaskFailed
                        type: string
                      type: array
                    exitCode:
                      description: 'The exit code of the pod container, controller
                        will take action according to this code. Note: only one of
                        `Event` or `ExitCode` can be specified.'
                      format: int32
                      type: integer
                    timeout:
                      description: Timeout is the grace period for controller to take
                        actions. Default to nil (take action immediately).
                      type: string
                  type: object
                type: array
              priorityClassName:
                description: If specified, indicates the job's priority.
                type: string
              queue:
                description: Specifies the queue that will be used in the scheduler,
                  "default" queue is used this leaves empty.
                type: string
              runningEstimate:
                description: Running Estimate is a user running duration estimate
                  for the job Default to nil
                type: string
              schedulerName:
                description: SchedulerName is the default value of `tasks.template.spec.schedulerName`.
                type: string
              tasks:
                description: Tasks specifies the task specification of Job
                items:
                  description: TaskSpec specifies the task specification of Job.
                  properties:
                    dependsOn:
                      description: Specifies the tasks that this task depends on.
                      properties:
                        iteration:
                          description: This field specifies that when there are multiple
                            dependent tasks, as long as one task becomes the specified
                            state, the task scheduling is triggered or all tasks must
                            be changed to the specified state to trigger the task
                            scheduling
                          type: string
                        name:
                          description: Indicates the name of the tasks that this task
                            depends on, which can depend on multiple tasks
                          items:
                            type: string
                          type: array
                      type: object
                    maxRetry:
                      description: Specifies the maximum number of retries before
                        marking this Task failed. Defaults to 3.
                      format: int32
                      type: integer
                    minAvailable:
                      description: The minimal available pods to run for this Task
                        Defaults to the task replicas
                      format: int32
                      type: integer
                    name:
                      description: Name specifies the name of tasks
                      type: string
                    policies:
                      description: Specifies the lifecycle of task
                      items:
                        description: LifecyclePolicy specifies the lifecycle and error
                          handling of task and job.
                        properties:
                          action:
                            description: The action that will be taken to the PodGroup
                              according to Event. One of "Restart", "None". Default
                              to None.
                            type: string
                          event:
                            description: The Event recorded by scheduler; the controller
                              takes actions according to this Event.
                            enum:
                            - '*'
                            - PodPending
                            - PodRunning
                            - PodFailed
                            - PodEvicted
                            - Unknown
                            - TaskCompleted
                            - OutOfSync
                            - CommandIssued
                            - JobUpdated
                            - TaskFailed
                            type: string
                          events:
                            description: The Events recorded by scheduler; the controller
                              takes actions according to this Events.
                            items:
                              description: Event represent the phase of Job, e.g.
                                pod-failed.
                              enum:
                              - '*'
                              - PodPending
                              - PodRunning
                              - PodFailed
                              - PodEvicted
                              - Unknown
                              - TaskCompleted
                              - OutOfSync
                              - CommandIssued
                              - JobUpdated
                              - TaskFailed
                              type: string
                            type: array
                          exitCode:
                            description: 'The exit code of the pod container, controller
                              will take action according to this code. Note: only
                              one of `Event` or `ExitCode` can be specified.'
                            format: int32
                            type: integer
                          timeout:
                            description: Timeout is the grace period for controller
                              to take actions. Default to nil (take action immediately).
                            type: string
                        type: object
                      type: array
                    replicas:
                      description: Replicas specifies the replicas of this TaskSpec
                        in Job
                      format: int32
                      type: integer
                    template:
                      $ref: '#/definitions/k8s.io~1api~1core~1v1~0PodTemplateSpec'
                      description: Specifies the pod that will be created for this
                        TaskSpec when executing a Job
                    topologyPolicy:
                      description: Specifies the topology policy of task
                      enum:
                      - none
                      - best-effort
                      - restricted
                      - single-numa-node
                      type: string
                  type: object
                type: array
              ttlSecondsAfterFinished:
                description: ttlSecondsAfterFinished limits the lifetime of a Job
                  that has finished execution (either Completed or Failed). If this
                  field is set, ttlSecondsAfterFinished after the Job finishes, it
                  is eligible to be automatically deleted. If this field is unset,
                  the Job won't be automatically deleted. If this field is set to
                  zero, the Job becomes eligible to be deleted immediately after it
                  finishes.
                format: int32
                type: integer
              volumes:
                description: The volumes mount on Job
                items:
                  description: VolumeSpec defines the specification of Volume, e.g.
                    PVC.
                  properties:
                    mountPath:
                      description: Path within the container at which the volume should
                        be mounted.  Must not contain ':'.
                      type: string
                    volumeClaim:
                      $ref: '#/definitions/k8s.io~1api~1core~1v1~0PersistentVolumeClaimSpec'
                      description: VolumeClaim defines the PVC used by the VolumeMount.
                    volumeClaimName:
                      description: defined the PVC name
                      type: string
                  required:
                  - mountPath
                  type: object
                type: array
            type: object
          status:
            description: Current status of the volcano Job
            properties:
              conditions:
                description: Which conditions caused the current job state.
                items:
                  description: JobCondition contains details for the current condition
                    of this job.
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one phase
                        to another.
                      format: date-time
                      type: string
                    status:
                      description: Status is the new phase of job after performing
                        the state's action.
                      type: string
                  required:
                  - status
                  type: object
                type: array
              controlledResources:
                additionalProperties:
                  type: string
                description: The resources that controlled by this job, e.g. Service,
                  ConfigMap
                type: object
              failed:
                description: The number of pods which reached phase Failed.
                format: int32
                type: integer
              minAvailable:
                description: The minimal available pods to run for this Job
                format: int32
                type: integer
              pending:
                description: The number of pending pods.
                format: int32
                type: integer
              retryCount:
                description: The number of Job retries.
                format: int32
                type: integer
              running:
                description: The number of running pods.
                format: int32
                type: integer
              runningDuration:
                description: The job running duration is the length of time from job
                  running to complete.
                type: string
              state:
                description: Current state of Job.
                properties:
                  lastTransitionTime:
                    description: Last time the condition transit from one phase to
                      another.
                    format: date-time
                    type: string
                  message:
                    description: Human-readable message indicating details about last
                      transition.
                    type: string
                  phase:
                    description: The phase of Job.
                    type: string
                  reason:
                    description: Unique, one-word, CamelCase reason for the phase's
                      last transition.
                    type: string
                type: object
              succeeded:
                description: The number of pods which reached phase Succeeded.
                format: int32
                type: integer
              taskStatusCount:
                additionalProperties:
                  description: TaskState contains details for the current state of
                    the task.
                  properties:
                    phase:
                      additionalProperties:
                        format: int32
                        type: integer
                      description: The phase of Task.
                      type: object
                  type: object
                description: The status of pods for each task
                type: object
              terminating:
                description: The number of pods which reached phase Terminating.
                format: int32
                type: integer
              unknown:
                description: The number of pods which reached phase Unknown.
                format: int32
                type: integer
              version:
                description: Current version of job
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
